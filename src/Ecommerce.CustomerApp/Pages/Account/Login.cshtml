@page
@model LoginModel
@{
  Layout = "_Layout";
}

<h2>Đăng nhập</h2>
<p>Bạn sẽ được chuyển hướng đến trang đăng nhập qua AuthServer.</p>
<a class="btn btn-primary" asp-page-handler="Start">Đăng nhập ngay</a>

@* @page
@model LoginModel
@{
  ViewData["Title"] = "Đăng nhập";
  Layout = "_AuthLayout"; // hoặc "_AuthLayout" nếu bạn tạo riêng layout cho auth
}


<div
  class="min-h-screen flex items-center justify-center bg-gradient-to-br from-pink-50 to-pink-100 dark:from-gray-900 dark:to-gray-800 px-4">
  <div class="w-full max-w-md bg-white dark:bg-gray-900 rounded-2xl shadow-xl px-8 py-10 space-y-6">

    <!-- Chữ logo đẹp -->
    <div class="text-center">
      <h1 class="text-3xl font-bold tracking-wide text-pink-600 dark:text-pink-400 font-[Quicksand]">Sweet Bakery</h1>
      <p class="text-sm text-gray-500 dark:text-gray-400 mt-1">Đăng nhập để thưởng thức bánh nhé 🍰</p>
    </div>

    <!-- Form -->
    <form method="post" class="space-y-5">
      <div>
        <label asp-for="Input.Username" class="block mb-1 text-sm font-medium text-gray-700 dark:text-gray-300">Tài
          khoản</label>
        <input asp-for="Input.Username" placeholder="Email hoặc tên đăng nhập"
          class="w-full px-4 py-2 rounded-lg border border-gray-300 dark:border-gray-600 bg-white dark:bg-gray-800 text-sm placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-pink-400 transition" />
      </div>

      <div>
        <label asp-for="Input.Password" class="block mb-1 text-sm font-medium text-gray-700 dark:text-gray-300">Mật
          khẩu</label>
        <input asp-for="Input.Password" type="password" placeholder="••••••••"
          class="w-full px-4 py-2 rounded-lg border border-gray-300 dark:border-gray-600 bg-white dark:bg-gray-800 text-sm placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-pink-400 transition" />
      </div>

      <div class="flex justify-between items-center text-sm">
        <div></div>
        <a href="#" class="text-pink-500 hover:underline">Quên mật khẩu?</a>
      </div>

      <button type="submit"
        class="w-full py-2 bg-pink-500 hover:bg-pink-600 text-white font-semibold rounded-lg shadow transition duration-200">
        Đăng nhập
      </button>
    </form>

    <!-- Đăng ký -->
    <p class="text-sm text-center text-gray-500 dark:text-gray-400">
      Chưa có tài khoản?
      <a href="/Account/Register" class="text-pink-500 hover:underline font-medium">Đăng ký ngay</a>
    </p>
  </div>
</div> *@


